# bitstream 与实测

本文给出发布的这份 bitstream 跑多快、在片上占多少资源，用来判断一次运行的耗时是否合理。

**文档模式**：实测数。

## 时钟

板上的 50 MHz 晶振接进 MMCM，分出 80 MHz 功能时钟，整颗 SoC（包括 2D 阵列）都跑在这一个时钟上。
片上互联与两路 DDR3 之间是异步的，MIG 那两个时钟各约 133 MHz。

综合与布局布线按 80 MHz 的 12.5 ns 周期收敛。布线后功能时钟的最差建立裕量 +0.384 ns，全片
最差建立裕量 +0.036 ns、最差保持裕量 +0.035 ns（都在 MIG 里），768,287 个端点全部满足。

## 一轮要多久

decode 一步是主机实测的墙钟。prefill 一轮的拍数由板上 CPU 自己报，秒数按 80 MHz 折算。

| 一轮 | 拍数 | 秒 |
| - | -: | -: |
| decode 一步（一个 token） | | 0.088 |
| prefill 一轮（64 个 token） | 51,952,270 | 0.65 |

decode 实测 11.38 token/s。

## 片上资源

器件是 `xc7k480tffg1156-2L`。下表是布线完报的数。

| 资源 | 用量 | 器件总量 | 占比 |
| - | -: | -: | -: |
| LUT | 212,399 | 298,600 | 71.13% |
| 　其中作查找表 | 189,088 | 298,600 | 63.32% |
| 　其中作分布式存储 | 23,311 | 108,600 | 21.47% |
| 触发器 | 152,434 | 597,200 | 25.52% |
| SLICE | 66,726 | 74,650 | 89.39% |
| BRAM（按 RAMB36 折算） | 789.5 | 955 | 82.67% |
| 　RAMB36 | 773 | 955 | 80.94% |
| 　RAMB18 | 33 | 1,910 | 1.73% |
| DSP48 | 1,660 | 1,920 | 86.46% |
| 引脚 | 242 | 400 | 60.50% |

## 片上布局

![按单元着色的布线后布局](placement.png)

这张图出自上一版 bitstream（50 MHz）的布局布线，不是发布的这一份，这一版的图待补。图例里的数字是各单元占用的 cell 数。2D 矩阵
单元与 1D 向量单元合计过半，外圈的 MIG 是 DDR3 控制器，右上角那一小块是 1GbE 的链路层。
